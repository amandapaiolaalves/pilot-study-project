[project]
name = "cpqd-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Amanda", email = "amandapaiolalaves@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cpqd-backend = "cpqd_backend:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
